<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">
  <StackLayout>
    <Label text="Tap the button" class="title"/>
    <Button text="juyjyjy" tap="{{ onTap }}" />
    <Label text="gegege" class="message" textWrap="true"/>

    <GridLayout width="100" height="100" backgroundColor="blue">
    </GridLayout>
    <!-- make changes to XML or CSS and save while running livesync --watch on android and windows -->
    <!-- it works occasionly but most of the times wont update the ui at all (meanwhile files updated in platforms thou) -->
    <!-- even ctrol + C and new livsync won't work everytime -->
    <GridLayout width="100" height="100" backgroundColor="green">
    </GridLayout>
  </StackLayout>
</Page>