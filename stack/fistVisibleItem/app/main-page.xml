<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="onLoaded">
  <ListView id="list" items="{{ myItems }}" itemTap="listViewItemTap">
      <ListView.itemTemplate>
        <Label text="{{ title || 'Downloading...' }}" textWrap="true" class="title" />
      </ListView.itemTemplate>
  </ListView>
</Page>