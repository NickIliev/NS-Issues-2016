<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="onLoaded" xmlns:lv="nativescript-telerik-ui/listview">

        <lv:RadListView id="myListView" items="{{ dataItems }}" >
            <lv:RadListView.listViewLayout>
                <lv:ListViewStaggeredLayout scrollDirection="Vertical" spanCount="3"/>
            </lv:RadListView.listViewLayout>
            <lv:RadListView.itemTemplate>
                <StackLayout orientation="vertical">
                    <Label fontSize="20" text="{{ itemName }}"/>
                    <Label fontSize="14" text="{{ itemDescription }}"/>
                </StackLayout>
            </lv:RadListView.itemTemplate>
        </lv:RadListView>
        <!--<ListView items="{{ dataItems }}" id="myListView">
            <ListView.itemTemplate>
                <StackLayout orientation="vertical">
                    <Label fontSize="20" text="{{ itemName }}"/>
                    <Label fontSize="14" text="{{ itemDescription }}"/>
                </StackLayout>
            </ListView.itemTemplate>
        </ListView>-->

</Page>